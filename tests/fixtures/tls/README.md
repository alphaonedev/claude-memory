# TLS test fixtures

PEM corpus for `src/tls.rs` and `tests/tls_integration.rs`. Regenerate with `./regenerate.sh` (delegates to `cargo run --example gen_tls_fixtures`). The hand-authored `allowlist_*.txt` files are committed verbatim and are not regenerated by the script.
